# Generated by run-example.(ps1|sh) — each example writes only under its own out/.
**/out/
# Serve-mode runs keep the status ledger (-Dstatus.backend=db, the default) beside the config dir.
**/inspecto-status.db
**/inspecto-status.db.wal
