# Duckle concept candidates — TRIAGE LIST, nothing decided, nothing built

**Status:** CANDIDATES (2026-09-14). This is a review list, not a plan and not BACKLOG rows.
The operator will add / remove / prioritise here first; only survivors move to
`docs/BACKLOG.md` §3. Until then nothing in this file is committed work.

**Upstream:** [slothflowlabs/duckle](https://github.com/slothflowlabs/duckle) — Rust + DuckDB,
self-hosted ETL, canvas or SQL authoring, plain-file workspace, headless runner + web console.
Beta v0.7.2, ~1.5k commits, MIT OR Apache-2.0. Same shape as inspecto; the value is in a
handful of crisply stated operational rules, not in its surface area.

**Explicitly out of scope (operator, 2026-09-14):** in-app git integration, component count,
UI languages, desktop/Tauri packaging, bundled local LLM.

**Grounding done before listing:** each item was checked against `inspecto*/src/main`,
`docs/okf/` and `docs/BACKLOG.md` so the "inspecto today" column is a grep result, not a guess.
Re-grep before acting — a row's premise goes stale (see `docs/PROJECT_NOTES.md`).

Vocabulary is canonical per `docs/GLOSSARY.md`: Pipeline, Dataset, Run, Incident,
Expectation, Alert Rule, Collector, Consignment.

---

## 1. Main candidates (rough payoff order)

| # | Concept (duckle's rule) | Inspecto today (grounded 2026-09-14) | Nearest inspecto home |
|---|---|---|---|
| C1 | **Dataset freshness SLA on a clock, not on failures.** An ownership rule declares `maximumAge` or `expectedAfterSchedule`. No declared limit ⇒ `unknown`, never `fresh`. A failed or *partial* run does not count as a refresh. A disabled/missing schedule makes the Dataset stale. `fresh→stale` alerts and `stale→fresh` sends an all-clear through the same Alert Rules; the all-clear is never held by a cooldown. `stale_since` carried across evaluations. Evaluated once a minute on its own thread, not the scheduler's. | **Absent.** All 13 Java "freshness" hits are notification *delivery-status* freshness (`DeliveryStatusAdapter`, `EnrichmentConfig`), not Dataset age. Nearest relative: the completeness KPI (K1+K2). BACKLOG: 0 rows. | Alert Rule / Incident; Catalog Dataset badge; agent skill `OperationalTables` |
| C2 | **Run diff = grouped recorded facts + rule-derived explanations.** Compare two Run receipts by kind: code, runtime, invocation, inputs, execution, output. Every explanation line traces to a listed difference — no generated prose. "Not compared" is stated explicitly. Absent is not zero (a run that died at node 2 has no counts after it). Secrets compared as `***` / digest. | **Absent as a surface.** Receipts + ledgers exist (`RunArtifactStore`, provenance rows). Only "diff" hits are `inspecto-intelligence` component tools. | Run ledger; agent diagnosers (`HeuristicDiagnoser`, `ModelDiagnoser`) as consumers |
| C3 | **Dead-property refusal at validate time.** A config key no component reads fails validation with a stable code + near-name suggestion (no suggestion when nothing is close). Accepted-names doc is *generated from the same map the checker enforces*. Strict at validate, warning at run; `x-` keys round-trip untouched. | **Partial, inconsistent.** `ComponentStore` refuses unknown keys; `RecipeCompiler` comment admits other unknown keys "stay"; `ArgumentDeriver` silently drops. Several past silent-loss defects in PROJECT_NOTES are this class. | `ConfigSafetyValidator`; node attribute specs / `step-types.contract.json` |
| C4 | **Parameter provenance with override record.** Two surfaces binding one parameter: later wins (documented rule); receipt records `{source, overrode:[...]}`. Only a *differing* value counts as an override. `secret` is a declared type; replaced with `***` in history, never dropped (so "was a token supplied?" stays answerable). All problems reported at once with stable codes (`param:unknown`, `param:missing`, …); undeclared names refused, not ignored. | **Parameter contract SHIPPED** (job-parameter-contract). Override/provenance record on the receipt: absent. | Job parameter contract; Run receipt |
| C5 | **Releases = content-addressed control-plane snapshots.** `build / diff / activate / rollback`. Activation *materialises* content (so A/B/A really executes A,B,A) and refuses before mutating, reporting every problem at once. Uncommitted work is named, not discarded. Pointer swap is one rename. Rollback deliberately ungated. Every Run records the release it *started* under. Only hashes + connection *references* stored, never values. | BACKLOG has ~55 release/promotion mentions; design reference, not new scope. | Existing release/promotion rows; `release-workflow` skill |
| C6 | **Workspace policy that can only narrow.** Denies union, allowlists intersect, permissions AND; `mode` from the server file only. Enforced at plan time *and* at the point of the act (network: every hop + DuckDB itself; state mutation: every watermark/offset advance). Prefixes match at a path boundary, not as strings. A named policy file that cannot be read **refuses the run**. | **Parts exist:** `PathJail`, `ConfigSafetyValidator`, `DataRef`. The structural narrowing rule and "unreadable policy refuses" are absent. ⚠ The prefix-boundary rule is exactly the guard defect corrected in phase C §5.4. | Config safety; sealed sandbox; edition gating |
| C7 | **Affected + contracts check against a git revision.** Which Pipelines does a change reach, each carrying the chain that reached it; asset edges *and* parent→child ref edges (reverse direction); deleting a producer is a change; canvas geometry ignored; dynamic paths listed as uncertain. Contract verdicts depend on the *reader*: remove a read column = breaking; unread = "possibly breaking", never "compatible"; downstream-of-transform = "revalidate" tier (no column lineage to prove either way). | Lineage/impact code exists (42 Java, 25 TS hits) but no CI-shaped gate over a diff. Note: *reads git objects*, so tied to git presence — flag if the git-integration exclusion covers this. | Lineage; `docs/api` breaking-change record; CI |
| C8 | **Baseline QA Expectation.** Profile current input vs the **median** of last N *accepted* profiles (row count; per column null count/rate, distinct, min, max, mean). Limits in either direction, % or absolute. `groupBy` + `requireExistingGroups` catches a missing partition when totals look normal. Profile accepted **only if the whole run succeeds**. Explicit `accept` / `clear` ops, audited with the replaced value; a refused run still records its profile. | **Absent as an Expectation kind.** "baseline/drift" hits are file-sequence gaps, diagnosers, backup. | Expectation kinds; FileSequenceGaps is a cousin |
| C9 | **A watcher is not a run.** A polling session has its own identity (`lastPollAt`, `pollCount`, `lastError`); only a poll that moves rows or fails mints a Run, naming the session as parent. `pollCount − runCount` = quiet time. Killed watcher reconciled to `interrupted` on next start, like a receipt. | Collector loop conflates polls and runs. Cheap to separate. | Collector / Consignment scheduler |
| C10 | **Named execution pools are admission only.** A pool answers "may this start now"; it never widens threads/memory caps. A Pipeline may *choose* a pool, never define one the server lacks (unknown ⇒ `default`). Queued run gets a durable id immediately with `queueReason`; becomes `running` with `queueMs`. A supervisor (Plan) takes no slot — holding one while waiting for a child that needs the same pool deadlocks. Metric: free permits per pool. | Scale-out phase B lease (`RunLease`, fenced db lease) is the seam. Rule set is a design constraint for it. | `enterprise-scale-out-plan.md` phase B |

## 2. Smaller items worth a line

✅ **GROUNDED 2026-09-15 — every row below that asked "check ours" has been checked.** This section is no
longer a list of questions: each note now carries a verdict with file:line evidence, so triage starts from
facts. Outcomes: **1 CLOSED by building it** (S15, DST tests) · **3 already satisfied** (S7, S8, S14) ·
**4 real defects found and filed as BACKLOG rows** (`DATASET-SELF-TRIGGER-1`, `DATASET-PUBLISH-ON-FAILURE-1`,
`LEDGER-PRUNE-EATS-RESUME-STATE-1`, `PRUNE-PREVIEW-DRIFT-1`) · **2 more filed from §2's security pair**
(`ROUTE-UNGATED-DEFAULT-1` P1, `AUDIT-REFUSAL-GAP-1`) · the rest are genuine feature gaps that still need
the operator's strike/rank pass.

⚠ **Two rows' own premises were wrong, in opposite directions** — S9's "BACKLOG has 12 backfill mentions"
overstated coverage (none correspond to shipped code), while S13 understated the gap (there is no ownership
model at all, not merely a missing lint). ⛔ **Re-grep a row before ranking it**; that is the same lesson
`docs/PROJECT_NOTES.md` records as *a row's cause is a hypothesis*.


| # | Rule | Note for inspecto |
|---|---|---|
| S1 | **Retention deletes nothing by default;** opt-in per category. Saved state is *never* touched. Audit log never pruned by age. `--dry-run` and real prune share one planning function. | **GROUNDED 2026-09-15 — splits three ways.** 🔴 *state is never housekeeping*: **VIOLATED** — `highWatermark()` is derived from ledger fingerprints (no separate column) and `ledger_prune` deletes them ⇒ filed `LEDGER-PRUNE-EATS-RESUME-STATE-1` (P2). 🔴 *one planning function*: **PARTIAL** — partition tasks share one loop, store-backed tasks use two independent WHERE clauses, and `DedupPruneTask`'s dry run reports total size rather than a plan ⇒ filed `PRUNE-PREVIEW-DRIFT-1` (P3). ✅ *audit never pruned by age*: inspecto differs **BY DECISION, not oversight** — AUDIT events share the Parquet event store and `compliance/controls-matrix.md:102` states the one-year `event_prune` window covers them. Re-confirm if this rule is adopted. |
| S2 | **Last publication of an SLA-bearing Dataset is kept** regardless of the retention horizon — otherwise a 30-day window reports a 90-day SLA breached 45 days early. | ⏸ **BLOCKED on C1, confirmed 2026-09-15 — not actionable and not gradeable yet.** There is no Dataset freshness SLA to bear (C1 is Absent), so "SLA-bearing" selects nothing today and the rule cannot be violated or satisfied. ⚠ Keep it attached to C1 rather than ranking it separately: it is a *constraint on C1's design*, and the cheapest moment to honour it is while building C1 — retrofitting a retention exemption afterwards is the expensive order. |
| S3 | **Publication-driven subscriptions.** One successful run = one publication event. Failed runs publish nothing. Publication, delivery and consumer run recorded as three states. Deliveries are *derived*; no self-subscription. | 🔴 **GROUNDED 2026-09-15 — the comparison found TWO REAL DEFECTS, both filed.** `dataset.write` is a fire-and-forget Signal, not a publication ledger. (a) *failed runs publish nothing* — **VIOLATED**, `DATASET-PUBLISH-ON-FAILURE-1` (P2). (b) *no self-subscription* — **VIOLATED**, `DATASET-SELF-TRIGGER-1` (P2); ⛔ the obvious guard CANNOT FIRE because `producer` is a job name at one emit site and a processor id at the other. (c) *one event per run* — it is **per ASSET** today, and the derived-table path emits none. (d) *three states* — only publication is durable; delivery is not recorded. ⚠ *a new subscriber gets past publications* holds by a DIFFERENT mechanism: the event stream is live-only, but `connector: dataset` discovers pre-existing snapshot FILES, so the data catches up even though the event does not. |
| S4 | **Item-level checkpointing** for paid per-row calls: result stored *as it arrives*; identity = business key **and** whole input row **and** stage config; no key ⇒ whole row (the safe direction: costs reuse, never a wrong answer). Pruning explicit only. | **GROUNDED 2026-09-15 — GAP, greenfield.** No per-row result cache or checkpoint exists in the enrichment or pipeline lanes; the only checkpoint-shaped thing in `pipeline/` is `BranchCommitLog` (branch commits, not per-item results). ⇒ a re-run of an enrichment/AI step repeats every paid call. ⚠ Rank this against **actual per-row cost** — it only pays for itself where calls are metered, which today means the assistant/enrichment lanes rather than the ETL core. ⛔ Note the identity rule is the subtle half, not the storage: keying on the business key ALONE silently reuses a stale result when the row changed underneath it. |
| S5 | **A route with no entry in the permission table requires admin** — a later route is locked down, not left open. | 🔴 **GROUNDED 2026-09-15 — REAL GAP, filed as `ROUTE-UNGATED-DEFAULT-1` (P1).** Gating is **opt-in**: 331 registrations, 91 gated, **~223 ungated of which 75 MUTATE**. `CapabilityManifest.capabilityFor` documents `null` = "ungated" as legitimate, and `ControlApi.authorize`'s ABAC stage is empty on Standard. Verified example: `DELETE /spaces/{id}` (`SpaceRoutes.java:72`) has **no capability gate** — any authenticated caller on Standard can deregister a Space. ⚠ The 75 is NOT a defect count (the `/auth/*` routes ARE the login flow); which to gate is an operator call. |
| S6 | **Refusals audited as carefully as successes** (`audit --outcome denied`). `allowed` means permitted, not succeeded. Reads not audited (a polling dashboard must not bury the record). | **GROUNDED 2026-09-15 — 2 of 3 ALREADY SATISFIED, 1 GAP (filed `AUDIT-REFUSAL-GAP-1`, P2).** ✅ *allowed ≠ succeeded*: `AuditTrail.record` appends `(refused, HTTP <status>)` for ≥400 (`:51`). ✅ reads not audited: `classify` (`:123-128`) audits GET only for `/export`. 🔴 GAP: a capability **403** and an auth **401** throw past `routeDispatch`'s audit calls into `errorBoundary`, so the refusal the gate itself produces leaves **no trace**. |
| S7 | **One id all the way through:** receipt, history record and log lines share the run id; log lines matched on a `run_id` *field*, not text, so a run that mentions another is not reported as its log. | ✅ **GROUNDED 2026-09-15 — ALREADY SATISFIED, including the half this row doubted.** One `runId` from `JobService.newRunId` threads into `JobRun`, `RunContext` and `RunLogEntry.runId` (`RunLogEntry.java:10`); logs are one JSONL per run and the read path is a **file lookup keyed by the id** (`RunLogStore.java:45`), never a text search — so a run that merely mentions another's id cannot be served as its log. ⇒ strike this row. |
| S8 | **Typed parameters validated at one boundary** — every surface (UI, CLI, API, scheduler, agent) reaches substitution through one function. | ✅ **GROUNDED 2026-09-15 — SATISFIED for job parameters.** `ParameterResolver.resolve` has exactly ONE production call site (`JobService.java:1209`), and every surface — manual trigger, cron, signal, replay — funnels through `submitRun → runJob → executeRun` to it. ⚠ One honest caveat: SQL `$`-tokens are a **separate, deliberate** namespace (`com.gamma.query.Parameters:47`), and `ParameterResolver`'s own header calls consolidating the two future work. ⇒ SATISFIED as scoped; PARTIAL only if the rule is read to cover SQL text too. |
| S9 | **Partitioned backfills** — each slice is an ordinary durable Run with its own receipt; `status / retry --partition / cancel`; an interrupted slice reconciles to `interrupted`. | **GROUNDED 2026-09-15 — GAP, and the board's mentions are a MIRAGE.** ~60 `backfill` hits across `docs/` are either an unrelated internal migration (`ObjectService.backfillTagAssignments`) or roadmap prose; **none correspond to shipped code**. The only relative is `ReprocessCommand` — *"reprocessing is whole-batch only"* (`:16-21`), a one-shot CLI action on one known batch id that mints **no Run at all**. No per-slice Run identity exists. ⚠ Re-grep before ranking: this row's own "12 mentions" overstated existing coverage. |
| S10 | **Masking on previews, not on written data.** PII/secret masks apply to what an operator *looks at*; the sink is untouched. | **GROUNDED 2026-09-15 — the sink half holds, the preview half is a GAP.** ✅ No write-path masking exists at all (`quality.pii.mask` / `quality.compliance.redact` are `Status.PLANNED`, `ProcessorCatalog.java:109,111`), so "the sink is untouched" is satisfied *by absence*. 🔴 Preview rows reach the Studio client **completely untransformed** (`DbBrowserRoutes.java:309`, twin at `:257`); no masking in `QueryExecutor`, none client-side. ⚠ **The input already exists and is simply unread**: a `classification: 'PII'` field tag flows into the catalog (`MetadataGraphBuilder.java:408`) as pure documentation. ⛔ Not to be confused with secrets masking (`ConnectionProfile`, `SecretScrubber`) — a different axis, already handled. |
| S11 | **Per-node materialisation mode** (`auto / view / memory / disk`) and per-node retry with backoff and memory cap as *Advanced* properties. | **GROUNDED 2026-09-15 — GAP, but the HOST for it already exists.** The `advanced` tier is real and in use: `step-types.contract.json` declares advanced attributes on 4 of 16 step types (`acquisition` has 18, plus `transform.dedup`, `transform.filter`, `sink.persistent`). ⛔ But there is **no per-Step `retry`/`backoff`, no materialisation mode and no per-node memory cap**: retry exists only on the ACQUISITION config (`PipelineCompiler.java:262`, `PipelineLift.java:226` — a `PipelineConfig.Retry`), and is not even exposed as a step attribute. ⇒ this is additive feature work on an existing mechanism, not a redesign — but it is a FEATURE, so it stays a candidate for triage rather than something to build unbidden. |
| S12 | **Catalog rebuild honesty:** the saved graph records what it was built from; the console *says* "changed since this was built" rather than rebuilding (a viewer action must not write). Unnameable assets are *counted on every answer*. | **GROUNDED 2026-09-15 — the defect it guards against is ABSENT; so is the honesty.** ✅ No read path writes: the graph is a `volatile` in-memory cache built lazily on first GET (`MetadataGraphService.java:77`) and dropped by `invalidate()` — a cache fill, not a viewer-triggered write. ⚠ But that is because **nothing is persisted to be honest about**: `MetadataGraph` is a bare `record(nodes, edges)` (`:15`) with no built-at, source set or fingerprint, and there is no staleness message — inspecto silently rebuilds instead. 🔴 Unresolvable nodes are **silently dropped**, not counted (`MetadataGraphBuilder.java:173-174`, with no-op drops at `:206`, `:213`, `:154-166`); the one `resolved` boolean (`:278`) is per-node and never aggregated ⇒ a partial graph does look complete. |
| S13 | **Ownership rules: first match wins, globs; `lint` fails on rules that match nothing** (a renamed asset silently orphans a team). Unowned assets fail only under `--strict`. | **GROUNDED 2026-09-15 — GAP, and larger than the row implies: there is NO ownership model at all.** No person/team owner exists on any Catalog asset, Dataset or Pipeline. Every `owner` in the tree is one of two unrelated things — the owning **Space** in Exchange sharing (`ShareGrant.java:19`) or a per-Incident assignee (`OperationalObject.java:20,31`). `AlertRule` carries no owner/team/contact (`:60-62`); routing is a flat attached destination (`ChannelConfig(id,kind,target)`) and the auth-free core hardcodes the recipient to `"appUser"` (`NotificationRule.java:112-113`). ⇒ no pattern rules, no first-match-wins, no globs — so **no match-nothing lint is even expressible** yet. ⚠ This is a precondition for C1's owner-routed freshness alerts; rank the two together. |
| S14 | **Two limiters read one config.** Runner (sync condvar) and scheduler (async semaphore) share the same numbers — "two limiters each parsing their own config is how the two schedulers came to disagree about time zones." | ✅ **GROUNDED 2026-09-15 — ALREADY SATISFIED.** `scheduler.toon` is parsed in exactly one place (`SchedulerSettings.read`), and `SchedulerRoutes.installResourceCaps` (`:259-263`) pushes its two keys out to the two limiters — `ConcurrencyBroker.setSystemCap` and `JobService.installMaxConcurrentRuns`. Neither limiter reads the file itself, so there is no second parse to drift. ⚠ They are two **different numbers by design** (Consignment slots vs Run slots), not one number read twice. ⇒ strike this row. |
| S15 | **Cron in a named civil time zone with DST handling pinned and tested;** interval/file-watch schedules have no civil-time deadline. | ✅ **DONE 2026-09-15 — the one item here CLOSED rather than filed.** The mechanism was already right; the TESTS did not exist. Every cron test ran in **UTC** and `OperationsZoneTest`'s cross-midnight case in **Asia/Kolkata** — **neither zone has DST**, so the one behaviour a civil-zone cron exists to get right was unverified. Probed first, then pinned in `CronExpressionTest` (11 → 14 tests): spring-forward **skips** the hour that does not exist (a daily 02:30 job does not run on 2026-03-08 — a real operational consequence, now on the record); autumn-back fires **once, not twice**; and `next()` advances strictly across both transitions — the invariant that stops a DST edge case becoming a hot loop. |

## 3. Next step

⛔ **§1 (C1–C10) still needs the operator: strike rows, merge duplicates, rank.** Those are build/don't-build
calls on features, so nothing there was actioned — a candidate list is not authority to build.

✅ **§2 is DONE as a review pass (2026-09-15).** Every "check ours" note now carries a grounded verdict, so
the remaining decision there is only *whether to adopt the rule*, never *what is true today*. What that pass
produced:

| Outcome | Rows |
|---|---|
| **Closed by building it** | S15 — DST tests (mechanism was right, coverage was absent) |
| **Already satisfied — strike** | S7 (run id is a field, not a text match) · S8 (one parameter boundary) · S14 (one config, two limiters) |
| **Defect found → filed** | S3 → `DATASET-SELF-TRIGGER-1`, `DATASET-PUBLISH-ON-FAILURE-1` · S1 → `LEDGER-PRUNE-EATS-RESUME-STATE-1`, `PRUNE-PREVIEW-DRIFT-1` · S5 → `ROUTE-UNGATED-DEFAULT-1` (P1) · S6 → `AUDIT-REFUSAL-GAP-1` |
| **Feature gap — still needs ranking** | S4 · S9 · S10 · S11 · S12 · S13 |
| **Blocked** | S2 (on C1) |

🔴 **The review was worth more than the adoption would have been.** Six defects were found by *comparing*
inspecto against crisply stated rules — including a P1 (75 ungated mutating routes, `DELETE /spaces/{id}`
among them) and a self-triggering hot loop whose javadoc asserted it could not happen. ⇒ **the value of a
list like this is the grounding it forces, not the features it proposes.**

⚠ **Do not archive this file yet.** The lifecycle says archive once the work is distilled, and §1 is still
undecided. When §1 is triaged: move survivors to `BACKLOG.md` §3 (re-grepping "inspecto today" at filing
time — two rows here had wrong premises), then `git mv` this file to
`docs/archived-documents/plans-archive/` and run `graphify update .`.
