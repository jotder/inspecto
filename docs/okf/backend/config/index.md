# Config

How Inspecto is configured: TOON files (parsed via JToon) and the safety gate that validates writes.

# Concepts

* [TOON config](toon-config.md) - `ConfigCodec`, the three config file types, `PipelineConfigParser`, and the serialization gotcha.
* [Config safety](config-safety.md) - `ConfigSafetyValidator`: path jail, numeric bounds, output allow-list.
* [Configuration reference](configuration.md) - every TOON key, block by block (moved from the retired root-level `configuration.md`).
* [Parsing options reference](parsing-options-reference.md) - all parser knobs across every `parsing.frontend` token (moved from the retired root-level `parsing-options-reference.md`).
